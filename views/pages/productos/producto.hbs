<section class="py-12">
    <div class="container mx-auto text-center">
        <h2 class="text-3xl font-bold text-pink-600 mb-6">{{title}}</h2>
        <div class="grid grid-cols-1 gap-8 pb-8">
            <div class="bg-white shadow-xl p-6 rounded-md">
                <ul>
                    <li><strong>Nombre:</strong> {{producto.name}}</li>
                    <li><strong>Descripción:</strong> {{producto.description}}</li>
                    <li><strong>Precio:</strong> ${{producto.price}}</li>
                </ul>

            </div>
        </div>
        <div class="flex justify-center mt-4">
            <a href="/" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                <i class="fas fa-home"></i> Volver al Home
            </a>
        </div>
    </div>
</section>